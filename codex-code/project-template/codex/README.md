# Codex Config (Project Template)

Use this directory for Codex-specific configuration:
- Persona presets for each SDLC stage (requirements/design/tasks/code/system_test/uat/runtime).
- Command definitions mapping to `codex-sdlc-*` entrypoints.
- Optional hooks (context indicators, pre-commit reminders).

Keep these configs aligned with the solution design in `docs/design/codex_aisdlc/`.***
