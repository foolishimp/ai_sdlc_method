# Copilot Plugin Bundles

Placeholder bundles that group Copilot AISDLC packs for common scenarios:
- `startup-bundle/`
- `datascience-bundle/`
- `qa-bundle/`
- `enterprise-bundle/`

Each bundle should declare dependencies on the core methodology plus relevant skills/standards.
