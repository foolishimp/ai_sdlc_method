{
  "description": "AI SDLC Methodology lifecycle hooks",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'AI SDLC Session Context:' && if [ -f .ai-workspace/tasks/active/ACTIVE_TASKS.md ]; then echo 'Active Tasks:' && cat .ai-workspace/tasks/active/ACTIVE_TASKS.md | head -100; else echo 'No active tasks file'; fi"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f .ai-workspace/tasks/active/ACTIVE_TASKS.md ]; then MODIFIED=$(git status --porcelain 2>/dev/null | wc -l | tr -d ' '); if [ \"$MODIFIED\" -gt 2 ]; then echo \"$MODIFIED uncommitted changes. Consider: /aisdlc-checkpoint-tasks\"; fi; fi"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git commit' 2>/dev/null; then if ! echo \"$CLAUDE_TOOL_INPUT\" | grep -qE 'REQ-[A-Z]+-' 2>/dev/null; then echo 'Commit may be missing REQ tag. Consider: /aisdlc-commit-task'; fi; fi"
          }
        ]
      }
    ]
  }
}
